import os

# to the monkey patching if running in development only.
if __name__ == '__main__' \
        and 'SERVER_MODE' in os.environ \
        and os.environ['SERVER_MODE'] != 'production':
    from gevent import monkey
    monkey.patch_all()


from {{arg0}} import mainapp


socketio = mainapp.socketio
application = mainapp.app
app = socketio


def launch():
    socketio.run(application, host='0.0.0.0', port=7001, debug=True)


if __name__ == '__main__':
    launch()
