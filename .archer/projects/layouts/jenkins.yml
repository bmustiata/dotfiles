config:
  name: jenkins
  requires:
  - JENKINS_PREFIX
  - PROJECT_HOME
  commands:
    open-jenkins: |
      BRANCH_NAME=$(git current)
      xdg-open $JENKINS_PREFIX${BRANCH_NAME//\//%252F}
    show-jenkins: |
      BRANCH_NAME=$(git current)
      echo $JENKINS_PREFIX${BRANCH_NAME//\//%252F}

